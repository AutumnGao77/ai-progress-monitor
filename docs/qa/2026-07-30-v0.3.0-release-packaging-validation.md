# v0.3.0 发布包验包记录

## 结论

本文是 v0.3.0 的唯一发布证据记录。本地最终候选包的构建、独立解包检查和核心交互验收已通过；Release PR、CI、Tag、GitHub Release 与发布后回下载仍待执行。任何未真实执行的项目均保留“待执行”，不得用开发态 App、旧版本包或历史校验和代替。

## 本版范围

| 范围 | 说明 |
|---|---|
| 系统通知 | Pet 右键菜单新增“系统通知 > 开启 / 关闭”；偏好持久化；关闭不影响监控、三态、角标、气泡和聚焦；重新开启不补发历史通知 |
| IDE 窗口生命周期 | 项目型 IDE 使用统一窗口清单、窗口失效清理和精准聚焦；已实机覆盖 Zed 与 Visual Studio Code |
| 多工具状态 | 修复 WorkBuddy 运行态、Qoder/Qoder CN 持续阻塞、ChatGPT 已查看会话 15 分钟收口等回归 |
| 稳定性 | 通知更新使用并发保护；来源并发测试使用线程屏障直接验证，不再依赖共享 Runner 墙钟阈值 |
| portable 运行入口 | 解压后的 `monitor_command.py` 和 `doctor.py` 可从同级 `ai-progress-monitor.pyz` 导入运行模块；构建强制执行包内帮助和 E2E |

## 候选来源

| 项目 | 当前值 |
|---|---|
| 发布分支 | `codex/release-v0.3.0` |
| 功能合并 | PR #5，`770d447` |
| CI 稳定性修复 | PR #6，`ed468b8` |
| Release PR | 待创建 |
| 最终 `main` commit | 待合并后填写 |
| annotated tag | 待创建；必须为 `v0.3.0` 且指向最终 `main` commit |
| GitHub Release | 待创建；必须为非 Draft、非 Prerelease |

## 自动化与构建门禁

| 检查 | 结果 | 证据 |
|---|---|---|
| 版本一致性 | 通过 | `pyproject.toml` 与 `ai_progress_monitor.__version__` 均为 `0.3.0`；App plist 在双包构建后继续核对 |
| 定向发布测试 | 通过 | portable 修复相关 43 项通过；文档、macOS App 与发布包契约 40 项通过；敏感信息与 JS 检查通过 |
| 完整测试 | 通过 | 修复后 517 项通过，用时 299.849 秒 |
| 发布校验 | 通过 | 编译、App/Event/E2E/Bridge/Doctor 帮助、JS、通知参数和敏感信息检查全部通过，输出 `release-validation-ok` |
| 双包构建 | 通过 | 第二版候选输出 `release-artifact-ok`、`macos-release-ok`、`portable-release-ok`；构建内包后入口与 E2E 门禁通过 |
| PR CI | 待执行 | GitHub `Core validation` |
| 合并后 `main` CI | 待执行 | GitHub `Core validation` |

## 候选阻断缺陷与修复

| 项目 | 记录 |
|---|---|
| 发现方式 | 首次构建成功后，从新临时目录解压 portable ZIP，并运行包内 `scripts/e2e_smoke.py` |
| 失败现象 | `smoke-inline` 未进入 `/api/sessions`，E2E 报 `needs_action session not found` |
| 根因 | portable 包不包含 `src/`，但 `monitor_command.py` 与 `doctor.py` 只把不存在的 `portable/src` 加入导入路径，无法导入 pyz 内的 `ai_progress_monitor` |
| 构建门禁盲点 | 旧构建只用仓库源码版 `scripts/e2e_smoke.py` 验证 `dist/ai-progress-monitor.pyz`，没有运行 portable 包自带脚本 |
| 修复 | 开发态优先导入 `src/`；portable 解压态回退同级 `ai-progress-monitor.pyz`；构建后强制运行包内 `monitor_command.py --help`、`doctor.py --help` 与包内 E2E |
| 测试证据 | 3 个新增测试先因缺少能力失败，修复后相关 43 项、全量 517 项及发布校验全部通过 |
| 候选处理 | 首次 ZIP 和 SHA-256 作废；必须重新构建、重新解压、重新验包和重新计算哈希 |

## 预期产物与边界

| 产物 | 面向用户 | 必须包含 | 禁止包含 |
|---|---|---|---|
| `AI-Progress-Monitor-v0.3.0-macOS-arm64.zip` | macOS 13+ Apple Silicon | 一个 `AI Progress Monitor.app`、`README.txt`、`LICENSE` | 根目录 `.pyz`、`scripts/`、`native/`、Swift 源码、候选图片、`.DS_Store` |
| `ai-progress-monitor-v0.3.0-portable.zip` | Web/CLI 集成、诊断、Windows 轻量预览 | `.pyz`、`scripts/`、`native/windows/`、`README.txt`、`LICENSE` | 任意 macOS `.app` |

## macOS 候选包检查

| 检查 | 结果 | 证据 |
|---|---|---|
| ZIP 根目录结构 | 通过 | 仅一个 `AI Progress Monitor.app`、`README.txt`、`LICENSE` |
| App 版本 | 通过 | `CFBundleVersion=0.3.0`；`CFBundleShortVersionString=0.3.0` |
| 架构与系统 | 通过 | Mach-O 64-bit arm64；`LC_BUILD_VERSION minos 13.0` |
| 签名 | 通过 | `codesign --verify --deep --strict` 通过；签名明确为 ad-hoc，无 TeamIdentifier |
| 图标与资源 | 通过 | `app-avatar.png`、`AppIcon.icns`、三态 Pet、衬衫 Pet 均存在 |
| 包卫生 | 通过 | 无 Swift 构建源码、候选资源目录、`.DS_Store`；portable 无 macOS `.app` |

## 发布候选人工验收

| 场景 | 结果 | 真实证据 |
|---|---|---|
| 最终 ZIP 内 App 首次启动 | 通过（候选实测） | 从全新临时目录解压 macOS ZIP 并直接启动其中唯一 App；原生 WebView 完成加载，Pet 图片自然尺寸为 `768 x 768`、页面非空，菜单栏状态项可打开 `Show Monitor / Quit`，页面控制台错误和警告均为 0 |
| 系统通知关闭 | 通过（候选实测 + 既有系统计数） | 真实原生 Pet 菜单选择“关闭”后再次展开显示“开启 / ✓ 关闭”，接口与隔离偏好均为关闭；随后注入新的运行中和待处理状态，气泡总数、三态图片与角标继续更新。关闭时 0 条系统通知、开启时 1 条的通知中心精确计数沿用 2026-07-21 同一实现的独立实机证据 |
| 重启保持 | 通过（候选实测） | 正常退出最终 ZIP 内 App，以同一隔离偏好重新启动；真实原生菜单仍显示“开启 / ✓ 关闭”，配置保持合法 JSON |
| 重新开启 | 通过（候选实测 + 自动化） | 重启后在真实原生菜单选择“开启”，再次展开显示“✓ 开启 / 关闭”，落盘恢复为 `true`；关闭期间旧状态不补发和后续新状态恢复通知由通知管理自动化与 2026-07-21 通知中心实测共同证明 |
| `--no-notifications` | 通过（候选实测） | 用最终 portable 包内 pyz 和同一隔离偏好启动；接口返回实际关闭且锁定，页面显示“开启 / ✓ 关闭”，两个选项均不可点击；进程退出后配置文件仍为 `notifications_enabled: true`，未被启动参数改写 |
| Pet 原有交互 | 通过（候选实测 + 既有拖动证据） | 最终候选中实际覆盖空闲、运行中、待处理、角标、气泡、默认/衬衫外观、原生隐藏、菜单栏恢复和正常退出；拖动使用 2026-07-28 最终 Dev App 的真实约 30 像素往返拖动证据及本轮 517 项回归，没有冒充本次重复手动拖动 |
| Zed / VS Code | 通过（既有实机 + 候选回归） | 2026-07-23 至 2026-07-29 已完成 Zed 与 VS Code 的真实双窗口精准聚焦、关闭单窗清理和剩余窗口复聚焦；本次候选运行时代码未改动该链路，517 项完整回归与 Swift/发布门禁均通过，未冒充在最终 ZIP 中重新制造两个 IDE 现场 |
| ChatGPT / Qoder / WorkBuddy | 通过（既有实机 + 候选回归） | PR #5 前已分别完成 ChatGPT 15 分钟收口、Qoder CN 持续阻塞和 WorkBuddy 运行态真实 Dev App 复测；本次 517 项完整回归覆盖三态、阻塞、查看确认和 15 分钟规则，最终候选现场还识别到桌面/CLI 空闲会话，但未冒充重新发起三种工具的全部真实任务 |

## SHA-256

| 文件 | 本地候选 | GitHub 回下载 |
|---|---|---|
| `AI-Progress-Monitor-v0.3.0-macOS-arm64.zip` | `2b2354596bfb15674c7dbbceba7e75799e5e7ccab0dff5281e484deb37aaa7ec` | 待发布后下载 |
| `ai-progress-monitor-v0.3.0-portable.zip` | `6828472624fca3cf146efb38aa8775a118742bdd01157dee49b1107396bec382` | 待发布后下载 |

## 发布后确认

| 检查 | 结果 |
|---|---|
| Tag 指向最终 `main` commit | 待执行 |
| Release 非 Draft / Prerelease | 待执行 |
| 两个附件名称和大小正确 | 待执行 |
| GitHub 回下载 SHA-256 等于本地候选 | 待执行 |
| 回下载 macOS App 启动与核心路径 | 待执行 |

## 已知发布约束

- macOS App 使用 ad-hoc 签名，尚未经过 Apple notarization。
- macOS 用户包只支持 Apple Silicon arm64 和 macOS 13+。
- 运行时需要 Python 3.9+。
- Windows 原生悬浮入口仍是 portable 包中的轻量预览，不属于本版稳定验收平台。
